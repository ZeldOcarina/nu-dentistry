---
label: Map image
hide_body: true
fields:
- name: block
  type: text
  config:
    required: false
  label: Block
  default: map-image
  hidden: true
- name: link
  type: text
  config:
    required: false
  label: Link
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
